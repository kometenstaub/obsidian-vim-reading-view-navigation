Replace throughout the repo: 
{{plugin-name}}
{{plugin-id}}
{{plugin-desc}}
{{plugin-class}}

---

add/remove style.css from the realease.yml

---
to copypaste into the obsidian releases json:

    },
    {
        "id": "{{plugin-id}}",
        "name": "{{plugin-name}}",
        "author": "pseudometa",
        "description": "{{plugin-desc}}",
        "repo": "chrisgrieser/{{plugin-id}}"
    }
